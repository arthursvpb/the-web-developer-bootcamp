<h1>Results page</h1>

<h2>Titles with that search:</h2>
<% data.Search.forEach(movie => { %>
<li><strong><%= movie.Title %></strong> - <%= movie.Year %></li>
<img height="100px" src="<%= movie.Poster %>" />
<% }); %>

<a href="/">Back</a>